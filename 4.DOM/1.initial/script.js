// Selecting an element.
'use strict'

 console.log(document.querySelector('.message').textContent);
 // Note : querySelector is method inside the document object because it have parenthesis. Method always have parenthesis.
 // And textContent is property.
 // Dot operator : Runs from left to right => operator precedence.
 

